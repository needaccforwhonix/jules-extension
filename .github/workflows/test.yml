name: Tests

on:
  workflow_call:

env:
  JULES_E2E_VSCODE_VERSION: "1.113.0"

jobs:
  test-unit:
    runs-on: ubuntu-latest
    env:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - node-version: "20.19.x"
            primary: true
          - node-version: "22.13.x"
            primary: false
          - node-version: "24.x"
            primary: false
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.28.0

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run unit tests
        if: matrix.primary == false
        run: pnpm run test:unit

      - name: Run unit tests with coverage
        if: matrix.primary
        run: pnpm run test:coverage

      - name: Verify JUnit report exists for Codecov
        id: verify-junit
        if: always() && matrix.primary
        shell: bash
        run: |
          if [[ ! -f ./test-results/junit.xml ]]; then
            echo "Expected JUnit report was not found at ./test-results/junit.xml"
            ls -la ./test-results || true
            exit 1
          fi

      - name: Upload test results to Codecov
        if: always() && matrix.primary && steps.verify-junit.outcome == 'success' && env.CODECOV_TOKEN != ''
        uses: codecov/codecov-action@v7
        with:
          token: ${{ env.CODECOV_TOKEN }}
          files: ./test-results/junit.xml
          report_type: test_results
      - name: Upload coverage to Codecov
        if: matrix.primary && env.CODECOV_TOKEN != ''
        uses: codecov/codecov-action@v7
        with:
          token: ${{ env.CODECOV_TOKEN }}
          files: ./coverage/lcov.info
          flags: unittests
          name: unit-node20
          fail_ci_if_error: true

  build-check:
    runs-on: ubuntu-latest
    env:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - node-version: "20.19.x"
            primary: true
          - node-version: "22.13.x"
            primary: false
          - node-version: "24.x"
            primary: false
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.28.0

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run build/package
        run: pnpm run package

      - name: Upload Codecov Bundle Analysis
        if: matrix.primary && env.CODECOV_TOKEN != ''
        run: npx @codecov/bundle-analyzer dist --bundle-name="jules-extension" --upload-token="${CODECOV_TOKEN}"

  integration:
    needs: [test-unit, build-check]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node-version: ["20.19.x", "22.13.x", "24.x"]
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.28.0

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml

      - name: Cache VS Code Test Downloads (.vscode-test)
        uses: actions/cache@v6
        with:
          path: |
            ~/.vscode-test
            ~/.vscode
            .vscode-test
          key: ${{ runner.os }}-vscode-test-${{ env.JULES_E2E_VSCODE_VERSION }}
          restore-keys: |
            ${{ runner.os }}-vscode-test-

      - name: Ensure display available
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y xvfb

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Compile
        run: pnpm run compile

      - name: Run tests (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: xvfb-run -a pnpm test

      - name: Run tests (macOS/Windows)
        if: runner.os != 'Linux'
        shell: bash
        run: pnpm test

  e2e-smoke:
    needs: [test-unit, build-check]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.28.0

      - name: Setup Node.js 20.19
        uses: actions/setup-node@v7
        with:
          node-version: "20.19.x"
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml

      - name: Cache VS Code Test Downloads (.vscode-test)
        uses: actions/cache@v6
        with:
          path: |
            ~/.vscode-test
            ~/.vscode
            .vscode-test
          key: ${{ runner.os }}-vscode-test-${{ env.JULES_E2E_VSCODE_VERSION }}
          restore-keys: |
            ${{ runner.os }}-vscode-test-

      - name: Ensure display available
        run: sudo apt-get update && sudo apt-get install -y xvfb

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Compile
        run: pnpm run compile

      # Run the E2E smoke test on Linux in this dedicated job
      - name: Run E2E smoke test
        shell: bash
        run: xvfb-run -a pnpm run test:e2e
